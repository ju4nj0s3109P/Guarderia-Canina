defmodule Guarderia.Repo.Migrations.CreateReservasMascotas do
  use Ecto.Migration

  def change do
    create table(:reservas_mascotas, primary_key: false) do
      add :id, :uuid, primary_key: true, default: fragment("gen_random_uuid()")

      add :reserva_id, references(:reservas, type: :uuid, on_delete: :delete_all), null: false
      add :mascota_id, references(:mascotas, type: :uuid, on_delete: :delete_all), null: false

      add :precio_individual, :decimal

      timestamps()
    end

    create unique_index(:reservas_mascotas, [:reserva_id, :mascota_id])
  end
end
